--- a/locate/Makefile.am
+++ b/locate/Makefile.am
@@ -65,9 +65,6 @@ updatedb: updatedb.sh Makefile
 	$(srcdir)/updatedb.sh > $@
 	chmod +x $@
 
-install-data-hook:
-	$(top_srcdir)/build-aux/mkinstalldirs $(DESTDIR)$(localstatedir)
-
 dblocation.texi:
 	echo '@set LOCATE_DB $(LOCATE_DB)' > $@.tmp
 	if test -f $@ && cmp $@.tmp $@ >/dev/null ; then \
